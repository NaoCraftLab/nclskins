org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# A root clean must leave no root build directory. Gradle's configuration-cache report
# is written to build/ after task execution, so it would otherwise recreate that directory.
org.gradle.configuration-cache=false
